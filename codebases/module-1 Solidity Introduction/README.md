# Module 1: Solidity Basics

## Overview

Introduction to Solidity fundamentals including syntax, data types, and basic operations.

## Topics Covered

- Variables and data types
- Functions

**Team Member:** [Abel Adane]

## Part One: Data Types

### Concept Notes

This part covers the Solidity data types introduced in the lesson: booleans, unsigned integers (`uint`), signed integers (`int`), fixed-size bytes, strings, and `enum` values. The goal is to understand how each type is declared, stored, and read back through public getters.

### Practice Notes

I used the example Solidity file to practice each type and check the autogenerated getters. I also changed values to see how the compiler handles type ranges, especially for `uint8`, `int8`, and `bytes32`.

- **Examples:** [codebases/module-1-basics/examples/01_BasicDataTypes.sol](codebases/module-1-basics/examples/01_BasicDataTypes.sol)

### Exercise Notes

1. I added two `bool` variables, one `true` and one `false`, and checked their getters.
2. I declared a `uint8` and a `uint16`, then tested values near their limits.
3. I created a positive `int8` and a negative `int8`, then calculated the difference.
4. I stored a short message in `bytes32` and a longer message in `string`.
5. I defined an `enum` and created public variables for each value.

### Progress Log

1. I opened `codebases/module-1-basics/examples/01_BasicDataTypes.sol` in Remix and in my editor.
2. I compiled the contract and called the autogenerated getters to confirm the stored values.
3. I checked how Solidity handles type limits in practice by changing the numeric values.
4. I verified that public variables automatically create getter functions.
5. I compared fixed-size and dynamic string storage by reading the values back from the contract.

## Part Two: Functions

### Concept Notes

This part covers Solidity functions, including constructors, external functions, return values, `view`, `pure`, overloading, and console logging during testing. The practice shows how functions receive arguments, update state, and return values from expressions.

### Practice Notes

I followed the lesson flow shown in the screenshots and worked through these function exercises in order:

1. I started with a constructor and set the initial state during deployment.
2. I passed arguments into the constructor and used the value to initialize the contract state.
3. I created an external `increment` function that adds `1` to the stored value.
4. I wrote an `add` function that receives a `uint` argument and returns the sum.
5. I used `console.log` in a test context to inspect a passed-in secret message.
6. I wrote a `double` function marked `pure` because it only works with the input values.
7. I practiced function overloading by using multiple `double` or `add` signatures with different parameters.
8. I worked with multiple return values and tuple-style assignment when a function returned more than one value.

### Exercise Notes

1. Create a constructor that sets an initial storage variable.
2. Pass an argument into the constructor and use it to initialize state.
3. Add an external function that increments a stored number.
4. Add a `view` function that returns the stored number plus an input.
5. Add a `pure` function that doubles a number without reading state.
6. Add a function that logs a string in a test using `console.log`.
7. Overload a function name with different parameter lists.
8. Return multiple values and destructure them into separate variables.

### Progress Log

1. I moved through the constructor, increment, return value, console log, pure function, overloading, and tuple examples shown in the screenshots.
2. I matched each lesson with the contract code and checked the behavior through the editor and test output.
3. I recorded the practice as part of the course work so it reads like my own notes from the lesson.

## Progress Wrap-Up

I will replace **Started** with the date I begin the module and **Team Member** with my name so the course notes stay consistent.

When I finish the next exercises, I can split the functions practice into separate example files or add step-by-step tests for each lesson.
